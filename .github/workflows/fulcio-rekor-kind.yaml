name: Fulcio&Rekor E2E Tests

on:
  pull_request:
    branches: [ main ]

permissions: read-all

defaults:
  run:
    shell: bash
    working-directory: ./src/github.com/sigstore/scaffolding

concurrency:
  group: fulcio-rekor-kind-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  fulcio-rekor-ctlog-tests:
    name: e2e tests
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false # Keep running if one leg fails.
      matrix:
        k8s-version:
        - v1.22.x
        - v1.23.x
        - v1.24.x

        leg:
        - fulcio rekor ctlog e2e

        go-version:
        - 1.18

    env:
      GOPATH: ${{ github.workspace }}
      GO111MODULE: on
      GOFLAGS: -ldflags=-s -ldflags=-w
      KO_DOCKER_REPO: registry.local:5000/knative
      KOCACHE: ~/ko
      COSIGN_EXPERIMENTAL: true

    steps:
    - uses: chainguard-dev/actions/setup-mirror@main
    # https://github.com/mvdan/github-actions-golang#how-do-i-set-up-caching-between-builds

    - name: Set up Go
      uses: actions/setup-go@84cbf8094393cdc5fe1fe1671ff2647332956b1a # v3.2.0
      with:
        go-version: ${{ matrix.go-version }}
        check-latest: true

    - name: Check out our repo
      uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b # v3.0.2
      with:
        path: ./src/github.com/sigstore/scaffolding

    - uses: actions/cache@v3
      with:
        # In order:
        # * Module download cache
        # * Build cache (Linux)
        path: |
          ~/go/pkg/mod
          ~/.cache/go-build
          ${{ env.KOCACHE }}
        key: ${{ runner.os }}-go-${{ matrix.go-version }}-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-${{ matrix.go-version }}-

    - uses: imjasonh/setup-ko@78eea08f10db87a7a23a666a4a6fe2734f2eeb8d # v0.4
      with:
        version: tip

    - name: Check out cosign repo so we get the head of the main cosign
      uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b # v3.0.2
      with:
        repository: sigstore/cosign
        path: ./src/github.com/sigstore/cosign

    - name: Install cosign
      working-directory: ./src/github.com/sigstore/cosign
      run: |
        go install ./cmd/cosign

    - name: Setup Cluster
      uses: chainguard-dev/actions/setup-kind@main
      id: kind
      with:
        k8s-version: ${{ matrix.k8s-version }}
        registry-authority: registry.local:5000
        cluster-suffix: cluster.local

    - name: Setup Knative
      uses: chainguard-dev/actions/setup-knative@main
      with:
        version: "1.2.0"
        serving-features: >
          {
            "kubernetes.podspec-fieldref": "enabled"
          }

    - name: Create sample image
      run: |
        pushd $(mktemp -d)
        go mod init example.com/demo
        cat <<EOF > main.go
        package main
        import "fmt"
        func main() {
          fmt.Println("hello world")
        }
        EOF
        demoimage=`ko publish -B example.com/demo`
        echo "demoimage=$demoimage" >> $GITHUB_ENV
        echo Created image $demoimage
        popd

    - name: Install Trillian
      working-directory: ./src/github.com/sigstore/scaffolding
      timeout-minutes: 5
      run: |
        make ko-apply-trillian

        # Make sure the Trillian jobs complete
        kubectl wait --timeout 2m -n trillian-system --for=condition=Complete jobs --all
        kubectl delete jobs -n trillian-system --all

    - name: Install Rekor
      working-directory: ./src/github.com/sigstore/scaffolding
      timeout-minutes: 5
      run: |
        make ko-apply-rekor

        # Make sure the Rekor jobs complete
        kubectl wait --timeout 2m -n rekor-system --for=condition=Complete jobs --all
        kubectl delete jobs -n rekor-system --all

    - name: Install Fulcio
      working-directory: ./src/github.com/sigstore/scaffolding
      timeout-minutes: 5
      run: |
        make ko-apply-fulcio

        # Make sure the Fulcio jobs complete
        kubectl wait --timeout 2m -n fulcio-system --for=condition=Complete jobs --all
        kubectl delete jobs -n fulcio-system --all

    - name: Install CTLog
      working-directory: ./src/github.com/sigstore/scaffolding
      timeout-minutes: 5
      run: |
        make ko-apply-ctlog

        # Make sure the CTlog jobs complete
        kubectl wait --timeout 2m -n ctlog-system --for=condition=Complete jobs --all
        kubectl delete jobs -n ctlog-system --all

    - name: Setup tuf and start serving it
      working-directory: ./src/github.com/sigstore/scaffolding
      timeout-minutes: 5
      run: |
        make ko-apply-tuf

        kubectl -n ctlog-system get secrets ctlog-public-key -oyaml | sed 's/namespace: .*/namespace: tuf-system/' | kubectl apply -f -
        kubectl -n fulcio-system get secrets fulcio-pub-key -oyaml | sed 's/namespace: .*/namespace: tuf-system/' | kubectl apply -f -
        kubectl -n rekor-system get secrets rekor-pub-key -oyaml | sed 's/namespace: .*/namespace: tuf-system/' | kubectl apply -f -

        # Make sure the tuf jobs complete
        kubectl wait --timeout 2m -n tuf-system --for=condition=Complete jobs --all
        kubectl delete jobs -n tuf-system --all

    - name: Initialize cosign with our custom tuf root
      run: |
        kubectl -n tuf-system get secrets tuf-root -ojsonpath='{.data.root}' | base64 -d > ./root.json
        TUF_MIRROR=`kubectl -n tuf-system get --no-headers ksvc tuf | cut -d ' ' -f 4`
        echo "TUF_MIRROR=$TUF_MIRROR" >> $GITHUB_ENV
        # Then initialize cosign
        cosign initialize --mirror $TUF_MIRROR --root /tmp/root.json

    - name: Run signing job in k8s using kubernetes tokens in the cluster
      working-directory: ./src/github.com/sigstore/scaffolding
      run: |
        # Grab the secret from the ctlog-system namespace and make a copy
        # in our namespace so we can get access to the CT Log public key
        # so we can verify the SCT coming from there.
        kubectl -n ctlog-system get secrets ctlog-public-key -oyaml | sed 's/namespace: .*/namespace: default/' | kubectl apply -f -

        make ko-apply-sign-job

        kubectl wait --for=condition=Complete --timeout=90s job/sign-job

    - name: Check that an entry was created in Rekor
      working-directory: ./src/github.com/sigstore/scaffolding
      run: |
        make ko-apply-checktree

        kubectl wait --for=condition=Complete --timeout=90s job/checktree

    - name: Verify the image with cosign using kubernetes tokens in the cluster
      working-directory: ./src/github.com/sigstore/scaffolding
      run: |
        # Grab the secret from the fulcio-system namespace and make a copy
        # in our namespace so we can get access to the Fulcio public key
        # so we can verify against it.
        kubectl -n fulcio-system get secrets fulcio-secret -oyaml | sed 's/namespace: .*/namespace: default/' | kubectl apply -f -

        make ko-apply-verify-job

        kubectl wait --for=condition=Complete --timeout=180s job/verify-job

    - name: Install a Knative service for fetch tokens off the cluster
      run: |
        make ko-apply-gettoken
        sleep 2
        kubectl wait --for=condition=Ready --timeout=15s ksvc gettoken

    - name: Get the endpoints on the cluster
      run: |
        REKOR_URL=`kubectl -n rekor-system get --no-headers ksvc rekor | cut -d ' ' -f 4`
        echo "REKOR_URL=$REKOR_URL" >> $GITHUB_ENV

        FULCIO_URL=`kubectl -n fulcio-system get --no-headers ksvc fulcio | cut -d ' ' -f 4`
        echo "FULCIO_URL=$FULCIO_URL" >> $GITHUB_ENV

        CTLOG_URL=`kubectl -n ctlog-system get --no-headers ksvc ctlog | cut -d ' ' -f 4`
        echo "CTLOG_URL=$CTLOG_URL" >> $GITHUB_ENV

        ISSUER_URL=`kubectl get --no-headers ksvc gettoken | cut -d ' ' -f 4`
        echo "ISSUER_URL=$ISSUER_URL" >> $GITHUB_ENV
        OIDC_TOKEN=`curl -s $ISSUER_URL`
        echo "OIDC_TOKEN=$OIDC_TOKEN" >> $GITHUB_ENV

    - name: Sign with cosign from the action using k8s token
      run: |
        cosign sign --rekor-url ${{ env.REKOR_URL }} --fulcio-url ${{ env.FULCIO_URL }} --force --allow-insecure-registry ${{ env.demoimage }} --identity-token ${{ env.OIDC_TOKEN }}

    - name: Verify with cosign from the action using k8s token
      run: |
        cosign verify --rekor-url ${{ env.REKOR_URL }} --allow-insecure-registry ${{ env.demoimage }}

    - name: Collect diagnostics
      if: ${{ failure() }}
      uses: chainguard-dev/actions/kind-diag@84c993eaf02da1c325854fb272a4df9184bd80fc # main
      with:
        artifact-name: logs.${{ matrix.k8s-version }}
